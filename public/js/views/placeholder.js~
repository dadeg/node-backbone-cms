window.PlaceholderView = Backbone.View.extend({

    initialize:function () {
        this.render();
	_.bindAll(this);
    },

    render:function () {
        $(this.el).html(this.template());
        return this;
    }

});
